import re
from rich.prompt import Prompt
from rich.traceback import install
install(show_locals=True)

from rich import print
from rich.panel import Panel

import os
import subprocess

def convert_video_aspect_ratio(video_path, output_path):
    ...
